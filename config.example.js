/**
 * Copia este archivo como `config.js` y rellena con tu proyecto (Settings → API).
 * No subas `config.js` al repositorio (está en .gitignore).
 */
window.SUPABASE_URL = "https://TU-PROYECTO.supabase.co";
/** Clave publicable (anon / publishable) del panel de Supabase */
window.SUPABASE_ANON_KEY = "TU_CLAVE_PUBLICABLE";
